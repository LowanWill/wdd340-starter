<% if (title) { %>
    <h1>
        <%= title %>
    </h1>
    <% } else { res.redirect('/') } %>

        <%- messages() %>





            <% if (errors) { %>
                <ul class="notice">
                    <% errors.array().forEach(error=> { %>
                        <li>
                            <%= error.msg %>
                        </li>
                        <% }) %>
                </ul>
                <% } %>
                    <form id="delete-form" action="/reviews/deleteReview" method="post">
                        <P> Confirm delete review</P>


                        <div>
                            <label for="review_text">Review</label>
                            <textarea name="review_text" id="review_text" required readonly <%=locals.review_text
                                %>></textarea>
                        </div>


                        <button type="submit">Delete Review</button>


                        <input type="hidden" name="account_id" value="<%= locals.account_id %>">
                        <input type="hidden" name="inv_id" value="<%= locals.inv_id %>">
                    </form>